# SpocR vNext configuration (example)
# Location: samples/restapi/.env.example
# Precedence: CLI > ENV > .env > legacy spocr.json (fallback only until v5.0 when JSON is removed)

# Generator mode: legacy | dual | next
SPOCR_GENERATOR_MODE=dual

# Enable experimental System.CommandLine based CLI (0=off, 1=on)
SPOCR_EXPERIMENTAL_CLI=0

# Allow direct major upgrade bypassing bridge policy (leave commented normally)
# SPOCR_ALLOW_DIRECT_MAJOR=1

# Treat nullable warnings as errors (Phase escalation)
# SPOCR_STRICT_NULLABLE=1

# Enforce strict diff (activate exit codes 21-23 when implemented)
# SPOCR_STRICT_DIFF=1

# Default database connection (runtime only, not written back by 'spocr pull')
SPOCR_DB_DEFAULT=Server=localhost;Database=AppDb;Trusted_Connection=True;TrustServerCertificate=True;

# Optional explicit namespace root override (IMPORTANT: root ONLY)
# Do NOT append '.SpocR' or any schema here. The generator will compose:
#   <SPOCR_NAMESPACE>.SpocR.<SchemaPascalCase>
# Example: SPOCR_NAMESPACE=RestApi  =>  RestApi.SpocR.Dbo.UserListResult
# If you set MyCompany.Project.Data => MyCompany.Project.Data.SpocR.Samples.CreateUserInput
SPOCR_NAMESPACE=MyCompany.Project.Data

# Database individual parts (alternative to SPOCR_DB_DEFAULT)
# DB_SERVER=localhost
# DB_DATABASE=AppDb
# DB_USER=sa
# DB_PASSWORD=Your_strong_password_123!

# SPOCR_DB_IDENTIFIER is the logical runtime connection name (alias of legacy SPOCR_DB_DEFAULT)
# SPOCR_OUTPUT_DIR normally remains 'SpocR' (do not duplicate in SPOCR_NAMESPACE)
# SPOCR_FEATURE_FLAGS=NewParser,StrictNullability

# RestApi specific settings
SPOCR_SAMPLE_RESTAPI_DB=Data Source=localhost;Initial Catalog=SpocRSample;User ID=sa;Password=SpocR@12345;Pooling=False;Connect Timeout=30;Encrypt=False;Trust Server Certificate=True;Authentication=SqlPassword;Application Name=spocr-samples-restapi;Connect Retry Count=1;Connect Retry Interval=10;Command Timeout=30

# SPOCR_NO_UPDATE=true

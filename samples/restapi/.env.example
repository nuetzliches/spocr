#################################################################################################
# SpocR Bridge Phase (v4.5 → v5.0) – Authoritative .env Template
# Location: samples/restapi/.env.example
# CONFIG PRECEDENCE (highest first):
#   1. CLI arguments            (e.g. spocr generate --mode next)
#   2. Process Environment      (export/set SPOCR_* before running CLI)
#   3. .env file                (this file after you copy it to .env)
#   4. legacy spocr.json (READ-ONLY fallback for ConnectionString in dual|legacy) – removed in v5
# Copy this file to `.env` in the same directory and adjust values.
# Only documented keys below are supported. Unlisted / deprecated keys will be ignored and
# removed in v5.0. Future v5 preview keys are commented under the PREVIEW section.
#
# Golden Hash / Determinism Policy Phases:
#   Phase Bridge (v4.5): Informational diff + relaxed verify
#   Phase Cutover (>=60% core coverage): Strict verify + exit codes (21–23)
#   Phase Hardened (≥80% coverage): Possible strict-diff default
#
# Edit carefully; commit `.env` ONLY if it is meant to be shared (normally keep local & gitignored).
#################################################################################################

## CORE MODE CONTROL ###########################################################################
# Generation mode options:
#   legacy – only legacy generator (deprecated)
#   dual   – legacy + vNext (default during bridge)
#   next   – only vNext output (parity tests optional)
SPOCR_GENERATOR_MODE=dual

## OPTIONAL FEATURE FLAGS ######################################################################
# Experimental new System.CommandLine based CLI (0=off, 1=on)
SPOCR_EXPERIMENTAL_CLI=0

# Allow direct major upgrade bypassing bridge policy (normally keep commented)
# SPOCR_ALLOW_DIRECT_MAJOR=1

## AUTO-UPDATE CONTROL #########################################################################
# Skip auto-update check (accepted truthy values: 1 | true)
# SPOCR_NO_UPDATE=1

## DATABASE CONNECTION #########################################################################
# Connection string used by generator for schema pull / metadata.
SPOCR_GENERATOR_DB=Server=localhost;Database=AppDb;Trusted_Connection=True;TrustServerCertificate=True;

## NAMESPACE & OUTPUT ##########################################################################
# Root namespace (no `.SpocR` suffix; generator appends `.SpocR` + schema segments automatically).
SPOCR_NAMESPACE=MyCompany.Project.Data

# Relative output directory for generated code (default 'SpocR'). Override only if needed.
# SPOCR_OUTPUT_DIR=SpocR

## SCHEMA SELECTION ###########################################################################
# Positive allow-list of schemas to generate (central filtering). Example:
# SPOCR_BUILD_SCHEMAS=core,app,identity
# Empty / unset => all schemas except explicitly ignored.
# SPOCR_BUILD_SCHEMAS=

## TARGET FRAMEWORK HINT ######################################################################
# Preferred TFM for forward-only features (net8.0 | net9.0 | net10.0).
# SPOCR_TFM=net10.0

## GOLDEN HASH / DIFF #########################################################################
# (Preview) Strict diff / golden hash enforcement (activated post-cutover; DO NOT ENABLE YET)
# SPOCR_STRICT_DIFF=0
# SPOCR_STRICT_GOLDEN=0

## JSON / STREAMING (v5 PREVIEW) ###############################################################
# (Preview – v5) Enable JSON dual mode generation (raw + deserialize + elements + streaming)
# SPOCR_ENABLE_JSON_DUAL=0
# (Preview – v5) Enable row streaming helpers (IAsyncEnumerable based)
# SPOCR_ENABLE_ROW_STREAMING=0

## ANALYZER / DIAGNOSTICS (v5 PREVIEW) #########################################################
# (Preview – v5) Emit analyzer warnings for namespace conflicts / duplicate method names
# SPOCR_ENABLE_ANALYZER_WARNINGS=0

#################################################################################################
# End of template. Add LOCAL-ONLY overrides below this line if necessary.
#################################################################################################

#################################################################################################
# SpocR v4.5 (Bridge Phase) – Authoritative .env Template
# Location: samples/restapi/.env.example
# Precedence: CLI > Process ENV > .env > (temporary) legacy spocr.json (removed in v5.0)
# Copy this file to .env (same directory) and adjust values. Only documented keys below are
# supported in this release. Unlisted keys may be ignored or removed in future versions.
#################################################################################################

## Testing
SPOCR_TEST_CONNECTION_STRING=Data Source=localhost;Initial Catalog=SpocRSample;User ID=sa;Password=SpocR@12345;Pooling=False;Connect Timeout=30;Encrypt=False;Trust Server Certificate=True;Authentication=SqlPassword;Application Name=spocr-samples-restapi;Connect Retry Count=1;Connect Retry Interval=10;Command Timeout=30

## Core Mode Control ###########################################################################
# Generation mode: legacy | dual | next  (default=dual). 'dual' emits legacy + vNext outputs.
SPOCR_GENERATOR_MODE=dual

## Optional Feature Flags ######################################################################
# Experimental new System.CommandLine based CLI (0=off, 1=on)
SPOCR_EXPERIMENTAL_CLI=0

# Allow direct major upgrade bypassing bridge policy (normally keep commented)
# SPOCR_ALLOW_DIRECT_MAJOR=1

## Update / Auto-Update ########################################################################
# Skip auto-update check (SPOCR_NO_UPDATE or SPOCR_SKIP_UPDATE accepted values: 1|true)
# SPOCR_NO_UPDATE=1

## Database Connection #########################################################################
# Connection string used by generator for schema pull / metadata.
SPOCR_GENERATOR_DB=Server=localhost;Database=AppDb;Trusted_Connection=True;TrustServerCertificate=True;

## Namespacing & Output ########################################################################
# Root namespace (no .SpocR suffix; generator appends .SpocR and schema segments automatically)
SPOCR_NAMESPACE=MyCompany.Project.Data

# Relative output directory for generated code (default 'SpocR'). Override only if needed.
# SPOCR_OUTPUT_DIR=SpocR

## Schema Selection ###########################################################################
# Positive allow-list of schemas to generate (overrides IgnoredSchemas). Example:
# SPOCR_BUILD_SCHEMAS=core,app,identity
# Leer oder nicht gesetzt => alle Schemas außer explizit ignorierte (spocr.json IgnoredSchemas / Status=Ignore)
# SPOCR_BUILD_SCHEMAS=

## Target Framework Hint ######################################################################
# Preferred TFM for template variant selection (net8.0 | net9.0 | net10.0). Used for forward-only features.
# SPOCR_TFM=net10.0

#################################################################################################
# End of template. Add local-only overrides below this line if necessary.
#################################################################################################

# SPOCR_BUILD_SCHEMAS=SchemaA,SchemaB (optional positive allow-list; empty -> all except ignored)
